import 'package:flutter/material.dart';

class InvestorMetricsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text(' investor ...'),
    );
  }
}
